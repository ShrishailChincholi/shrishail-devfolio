{
  "name": "shri-portfolio",
  "version": "1.0.0",
  "description": "Shrishail's Full Stack Portfolio",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}